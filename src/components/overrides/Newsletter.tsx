import { SectionOverride } from "@faststore/core";
import styles from "../../sass/index.module.scss";

const override: SectionOverride = {
  section: "Newsletter",
  className: styles.myNewsletterClass,
};

export { override };
